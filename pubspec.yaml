name: leximon
description: "Leximon mobile app foundation inspired by WaifuAI."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+6

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  go_router: ^14.6.2
  flutter_svg: ^2.0.10+1
  lottie: ^3.5.1
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter_tts: ^4.2.5
  http: ^1.6.0
  in_app_purchase: ^3.3.0
  in_app_purchase_storekit: ^0.4.11+1
  intl: ^0.20.2
  device_info_plus: ^11.5.0
  android_id: ^0.5.1
  package_info_plus: ^8.3.1
  just_audio: ^0.10.6
  record: ^7.1.1
  youtube_player_iframe: ^6.0.2
  # 3.18.3+ fixes the iOS AuthenticationChallengeResponse native crash.
  webview_flutter_wkwebview: ^3.26.0
  shared_preferences: ^2.5.3
  firebase_core: ^4.11.0
  firebase_analytics: ^12.4.6
  firebase_messaging: ^16.4.1

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  carousel_slider: ^5.1.2
  speech_to_text: ^7.4.0
  image_picker: ^1.2.3
  path_provider: ^2.1.6
  permission_handler: ^12.0.3
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1
  google_mlkit_translation: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  drift_dev: ^2.34.0

dependency_overrides:
  # 2.6.0 uses objective_c FFI, which is not resolved correctly on the
  # current iOS Simulator native-asset build.
  path_provider_foundation: 2.5.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/splash.png
    - assets/images/leximon-owl.png
    - assets/images/leximon-owl-wave.png
    - assets/images/banner_header.png
    - assets/images/banner_choose_language.png
    - assets/images/bg_word_study.png
    - assets/images/bg_repetition_practice.png
    - assets/images/cup_done_practice.png
    - assets/images/cancel_icon_dialog.png
    - assets/images/bg_sentence.png
    - assets/images/card_progress_banner.png
    - assets/images/empty_word_vocabulary_collection.png
    - assets/images/owl_daily_home.png
    - assets/images/tab_personal_active.png
    - assets/images/tab_personal_inactive.png
    - assets/images/practice_listen/
    - assets/images/in_app_purchase/
    - assets/images/loading/
    - assets/images/owls/
    - assets/images/onboarding/
    - assets/images/grammar/
    - assets/images/ipa/
    - assets/images/bg_reading.png
    - assets/images/bg_reading_screen.png
    - assets/images/reading/
    - assets/data/vocabulary_test.json
    - assets/data/grammar/allpack/acqp0001.json
    - assets/data/grammar/allpack/acqp0002.json
    - assets/data/grammar/allpack/bcqp0001.json
    - assets/data/grammar/allpack/bcqp0002.json
    - assets/data/grammar/allpack/ecqp0001.json
    - assets/data/grammar/allpack/ecqp0002.json
    - assets/data/grammar/allpack/ecqp0003.json
    - assets/data/grammar/allpack/icqp0001.json
    - assets/data/grammar/allpack/icqp0002.json
    - assets/data/listens/01-ielts-listening/course-index.json
    - assets/data/listens/02-short-stories/course-index.json
    - assets/data/listens/03-spelling-names/course-index.json
    - assets/data/listens/04-numbers/course-index.json
    - assets/data/listens/06-conversations/course-index.json
    - assets/data/listens/07-toefl-listening/course-index.json
    - assets/data/listens/08-random-videos/course-index.json
    - assets/data/listens/09-ipa/course-index.json
    - assets/data/listens/10-toeic-listening/course-index.json
    - assets/data/listens/12-ted/course-index.json
    - assets/data/listens/13-stories-for-kids/course-index.json
    - assets/data/listens/14-news/course-index.json
    - assets/data/listens/18-medical-english-oet/course-index.json
    - assets/svgs/
    - assets/svgs/flags/
    - assets/svgs/challenge/
    - assets/svgs/ipa/
    - assets/svgs/onboarding/
    - assets/lotties/

  fonts:
    - family: M PLUS Rounded 1c
      fonts:
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Thin.ttf
          weight: 100
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Light.ttf
          weight: 300
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Regular.ttf
          weight: 400
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Medium.ttf
          weight: 500
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Bold.ttf
          weight: 700
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/m_plus_rounded_1c/MPLUSRounded1c-Black.ttf
          weight: 900

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
